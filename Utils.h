#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>
#include <iostream>
#include <algorithm>
#include <windows.h>
#include <stack>
#include <cstdlib>
#include <ctime>
#include <memory> 
#include <fstream>
#include <sstream>
using namespace std;

bool isDigit(const std::string& s);
bool isIn(vector <pair<int, int>> v, pair<int, int> p);

#endif // UTILS_H
